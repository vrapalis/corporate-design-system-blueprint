{
  "name": "root",
  "private": true,
  "workspaces": [
    "apps/*",
    "libs/*"
  ],
  "devDependencies": {
    "@commitlint/cli": "^17.1.2",
    "@commitlint/config-conventional": "^17.1.0",
    "@types/react": "^18.0.0",
    "cz-conventional-changelog": "^3.3.0",
    "husky": "^8.0.1",
    "lerna": "^5.5.1",
    "nx": "14.7.5"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  },
  "scripts": {
    "prepare": "husky install",
    "build:clients": "lerna run build --scope=angular-client --scope=react-client --scope=web-client",
    "deploy:ghpage": "rm -rf ./docs/* && mkdir -p docs/angular-client docs/react-client docs/web-client && npm run build:clients && cp -r apps/angular-client/dist/* docs/ && cp -r apps/react-client/build/* docs/react-client/ && cp -r apps/web-client/dist/* docs/web-client/"
  }
}
